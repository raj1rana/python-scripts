newfile = open("filebypython","w+") #this is to create a variable to store the file name and the operation that we will do on it

write = "wow this text is written by python script "     #this is the text that we need to write into the file stored in a variable

newfile.write(write)  # the newfile is the file and .write is the function that will write the file

newfile.read()

